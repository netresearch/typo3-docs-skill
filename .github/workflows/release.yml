name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e # v2.14.2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create typo3-docs skill package
        run: |
          mkdir -p dist-skill
          cp skills/typo3-docs/SKILL.md dist-skill/
          cp LICENSE dist-skill/
          [ -d "skills/typo3-docs/references" ] && cp -r skills/typo3-docs/references dist-skill/
          [ -d "skills/typo3-docs/scripts" ] && cp -r skills/typo3-docs/scripts dist-skill/
          [ -d "skills/typo3-docs/assets" ] && cp -r skills/typo3-docs/assets dist-skill/
          [ -d "skills/typo3-docs/templates" ] && cp -r skills/typo3-docs/templates dist-skill/
          [ -d "skills/typo3-docs/examples" ] && cp -r skills/typo3-docs/examples dist-skill/
          [ -f "skills/typo3-docs/checkpoints.yaml" ] && cp skills/typo3-docs/checkpoints.yaml dist-skill/
          cd dist-skill
          zip -r ../typo3-docs-skill-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../typo3-docs-skill-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create typo3-docs plugin package
        run: |
          mkdir -p dist-plugin
          # Include typo3-docs skill files
          mkdir -p dist-plugin/skills/typo3-docs
          cp skills/typo3-docs/SKILL.md dist-plugin/skills/typo3-docs/
          [ -d "skills/typo3-docs/references" ] && cp -r skills/typo3-docs/references dist-plugin/skills/typo3-docs/
          [ -d "skills/typo3-docs/scripts" ] && cp -r skills/typo3-docs/scripts dist-plugin/skills/typo3-docs/
          [ -d "skills/typo3-docs/assets" ] && cp -r skills/typo3-docs/assets dist-plugin/skills/typo3-docs/
          [ -d "skills/typo3-docs/templates" ] && cp -r skills/typo3-docs/templates dist-plugin/skills/typo3-docs/
          [ -d "skills/typo3-docs/examples" ] && cp -r skills/typo3-docs/examples dist-plugin/skills/typo3-docs/
          [ -f "skills/typo3-docs/checkpoints.yaml" ] && cp skills/typo3-docs/checkpoints.yaml dist-plugin/skills/typo3-docs/
          cp LICENSE dist-plugin/
          # Include plugin manifest
          [ -d ".claude-plugin" ] && cp -r .claude-plugin dist-plugin/
          [ -d "hooks" ] && cp -r hooks dist-plugin/
          [ -d "scripts" ] && cp -r scripts dist-plugin/ && find dist-plugin/scripts -name '__pycache__' -type d -exec rm -rf {} + 2>/dev/null || true
          cd dist-plugin
          zip -r ../typo3-docs-plugin-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../typo3-docs-plugin-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          files: |
            typo3-docs-skill-${{ steps.version.outputs.version }}.zip
            typo3-docs-skill-${{ steps.version.outputs.version }}.tar.gz
            typo3-docs-plugin-${{ steps.version.outputs.version }}.zip
            typo3-docs-plugin-${{ steps.version.outputs.version }}.tar.gz
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
